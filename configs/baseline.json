{
  "split": {
    "train_end": 2023,
    "val_year": 2024,
    "test_year": 2025
  },
  "history_laps": 5,
  "ridge_alpha": 2.0,
  "hist_gradient_boosting": {
    "max_iter": 350,
    "learning_rate": 0.05,
    "max_leaf_nodes": 31,
    "l2_regularization": 0.1,
    "random_state": 7,
    "loss": "absolute_error"
  },
  "notes": "Required baselines from the master spec: previous lap, rolling-five median, ridge regression and tree boosting. They share the full-context feature table, train-only preprocessing, sparse-feature filter and winsorized training target with RaceShift FFR so the comparison is fair. The tree model optimises absolute error because MAE is the headline metric."
}
