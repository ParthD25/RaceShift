import { BrainCircuit, Cpu } from 'lucide-react';
import { Panel } from '../components/Panel';
const cards=[['RaceShift FFR','Primary trainable','4 deep local layers','Forward-Forward only','Project code'],['Rolling-5 Median','Required baseline','No parameters','Deterministic','Built in'],['Ridge / Tree Baselines','Sanity controls','Tabular','Baseline only','scikit-learn'],['Frozen TS Foundation Models','Optional benchmark','External open weights','Zero-shot only','Research']];
export default function Models(){return <div className="page-stack"><div className="page-heading"><div><h1>Models</h1><p>Model registry and role assignment for the forecasting stack.</p></div></div><div className="model-grid">{cards.map(c=><Panel key={c[0]} title={c[0]} icon={<Cpu size={17}/>}><div className="model-role">{c[1]}</div><div className="detail-list compact"><div><span>Size</span><strong>{c[2]}</strong></div><div><span>Mode</span><strong>{c[3]}</strong></div><div><span>License</span><strong>{c[4]}</strong></div></div></Panel>)}</div><Panel title="Selection rule" icon={<BrainCircuit size={17}/>}><p className="prose">A model enters the production path only if it beats the naïve pace baseline and XGBoost on unseen future-season data, preserves calibration, and fits the Colab compute budget.</p></Panel></div>}
